# ---------------------------------------------------------------------------------------------------------------------
# This test file configures required inputs with values for testing purpose
# Most likely you won't use such values
# ---------------------------------------------------------------------------------------------------------------------
cluster_path = "."
base_path    = "."
base_dir     = "tests"
